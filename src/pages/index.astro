---
import BaseLayout from "../layouts/BaseLayout.astro";
---

<BaseLayout title="7WW7 — Home" description="7 For The World, The World For 7">
  <section
    class="relative w-screen left-1/2 right-1/2 -ml-[50vw] -mr-[50vw] min-h-[92vh] overflow-hidden"
  >
    <div class="absolute inset-0">
      <img
        src="/images/hero1.jpg"
        alt=""
        class="h-full w-full object-cover object-center scale-105 opacity-85"
        loading="eager"
        decoding="async"
      />
      <div
        class="absolute inset-0 bg-gradient-to-b from-black/35 via-black/25 to-[var(--bg)]"
      >
      </div>
      <div
        class="absolute inset-0 bg-[radial-gradient(ellipse_at_center,rgba(0,0,0,.08),rgba(0,0,0,.35))]"
      >
      </div>
    </div>

    <div
      class="relative mx-auto max-w-6xl px-4 sm:px-6 lg:px-8 pt-16 md:pt-24 pb-16"
    >
      <div class="grid gap-10 md:grid-cols-12 md:items-start">
        <div class="md:col-span-7">
          <div
            class="inline-flex items-center gap-2 rounded-full border border-white/20 bg-white/10 px-4 py-2 text-xs font-semibold text-white/90 backdrop-blur"
          >
            <span class="h-2 w-2 rounded-full bg-[var(--brand2)]"></span>
            A Continental Peace Engine • Swiss-anchored neutrality
          </div>

          <h1
            class="mt-6 font-display text-4xl leading-tight text-white md:text-6xl"
          >
            <span class="text-[var(--brand2)]">7</span> For The World,<br />
            The World For <span class="text-[var(--brand2)]">7</span>
          </h1>

          <p class="mt-5 max-w-2xl text-lg text-white/85">
            A global cooperation architecture connecting seven continental
            foundations to convert instability into
            <span class="font-semibold text-white"> innovation</span>,
            <span class="font-semibold text-white"> alignment</span>, and
            <span class="font-semibold text-[var(--brand2)]">
              shared prosperity</span
            >.
          </p>

          <div class="mt-8 flex flex-wrap gap-3">
            <a
              href="/work"
              class="rounded-full bg-[var(--brand2)] px-5 py-3 text-sm font-semibold text-[var(--ink)] hover:opacity-95 transition"
            >
              Explore our work
            </a>
            <a
              href="/about"
              class="rounded-full border border-white/25 bg-white/10 px-5 py-3 text-sm font-semibold text-white hover:bg-white/15 transition"
            >
              How it works
            </a>
          </div>

          <div class="mt-10 grid gap-3 sm:grid-cols-3">
            {
              [
                ["Weekly", "21st Hour → Midnight conclaves"],
                ["Monthly", "21st Hour on the 21st forums"],
                ["Annual", "Peace Day Summit — Sept 21"],
              ].map(([k, v]) => (
                <div class="rounded-2xl border border-white/15 bg-white/10 p-4 backdrop-blur">
                  <div class="text-xs font-semibold text-[var(--brand2)]">
                    {k}
                  </div>
                  <div class="mt-1 text-sm text-white/85">{v}</div>
                </div>
              ))
            }
          </div>
        </div>

        <div class="md:col-span-5">
          <div
            class="rounded-3xl border border-white/15 bg-white/10 p-5 backdrop-blur"
          >
            <div class="flex items-center justify-between">
              <div class="text-xs font-semibold text-white/80">Top stories</div>
              <a
                class="text-xs font-semibold text-white/80 hover:text-white"
                href="/ideas">View all →</a
              >
            </div>

            <div class="mt-4 grid gap-3">
              {
                [
                  [
                    "Equilibrium Metrics Dashboard",
                    "/about",
                    "Public indicators + Balance Impact Rating.",
                  ],
                  [
                    "Playground For The World",
                    "/about",
                    "Experiential diplomacy meets education & innovation.",
                  ],
                  [
                    "Cadence of Cooperation",
                    "/ideas",
                    "Weekly → monthly → annual rhythm that produces action.",
                  ],
                ].map(([t, href, d]) => (
                  <a
                    href={href}
                    class="group rounded-2xl border border-white/10 bg-black/10 p-4 hover:bg-black/15 transition"
                  >
                    <div class="font-display text-lg text-white">{t}</div>
                    <div class="mt-1 text-sm text-white/75">{d}</div>
                    <div class="mt-3 text-sm font-semibold text-[var(--brand2)]">
                      Read →
                    </div>
                  </a>
                ))
              }
            </div>
          </div>

          <div
            class="mt-4 rounded-3xl border border-white/10 bg-white/10 p-5 text-white/85 backdrop-blur"
          >
            <div class="text-xs font-semibold text-white/80">Our mission</div>
            <div class="mt-2 text-sm">
              Build a world where cooperation scales faster than
              conflict—through measurable equilibrium across continents.
            </div>
          </div>
        </div>
      </div>
    </div>
  </section>

  <section class="py-12">
    <div class="grid gap-8 lg:grid-cols-12 lg:items-center">
      <div class="lg:col-span-5">
        <div
          class="overflow-hidden rounded-3xl border border-black/5 bg-white shadow-sm"
        >
          <img
            src="/images/playground.jpg"
            alt="Collaborative work and shared prosperity"
            class="h-80 w-full object-cover"
            loading="lazy"
            decoding="async"
          />
        </div>
      </div>

      <div class="lg:col-span-7">
        <div class="rounded-3xl border border-black/5 bg-white p-8 shadow-sm">
          <div class="text-xs font-semibold text-[var(--brand)]">
            PLAYGROUND FOR THE WORLD
          </div>
          <h3 class="mt-2 font-display text-2xl">
            Turning cooperation into lived experience
          </h3>
          <p class="mt-3 text-[var(--muted)]">
            7WW7’s “Playground For The World” concept makes diplomacy tangible:
            a space where culture, education, innovation, and empathy accelerate
            real programs across continents.
          </p>
          <div class="mt-6 flex flex-wrap gap-3">
            <a
              href="/about"
              class="rounded-full bg-[var(--ink)] px-5 py-3 text-sm font-semibold text-white hover:opacity-90 transition"
              >Learn more</a
            >
            <a
              href="/work"
              class="rounded-full border border-black/10 bg-white px-5 py-3 text-sm font-semibold hover:bg-black/[0.03] transition"
              >Explore foundations</a
            >
          </div>
        </div>
      </div>
    </div>
  </section>
</BaseLayout>
